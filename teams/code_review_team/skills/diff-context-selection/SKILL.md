# Diff Context Selection

For large PRs, select:

```text
changed files
callers/callees
public API contracts
tests touching changed behavior
configs and migrations
security-sensitive paths
```

Avoid unrelated files.
