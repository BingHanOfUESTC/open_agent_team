# Dependency And Entrypoint Analysis

Analyze:

```text
package manifests
lockfiles
build scripts
runtime entrypoints
environment variables
test commands
dev server commands
deployment configs
```

Prefer exact commands from repo files.
